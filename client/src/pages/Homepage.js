import React from "react";
import NavBar from "../components/Home/NavBar (public)/index";

const Home = () => {
    return (
        <Container fluid>
            <NavBar>

            </NavBar>
        </Container>
    );
};

export default Home;

